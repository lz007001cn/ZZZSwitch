新增 GitHub Releases 在线更新：设置页会静默显示当前版本、最新版本和更新说明；下载完成后使用 GitHub 提供的 SHA-256 验证安装包完整性，再由独立 Updater 完成替换、重启和失败回滚。

修复测试构建误判同基础版本为新版的问题，精简设置页中的版本显示，并改进二级窗口拖动体验。重新发布版本统一将英文服名显示为 CN，并补全二级窗口顶部边框。

公开发布的 1.3.7 尚未包含在线更新组件，因此从公开 1.3.7 升级到 1.4.0 仍需手动下载。安装 1.4.0 后，后续兼容版本即可使用应用内更新。

Adds GitHub Releases updates with silent version information, release notes, SHA-256 package verification, a standalone updater, restart handling, and rollback. Also fixes test-build version comparison, uses CN consistently in English server labels, and restores the top border on secondary windows.
